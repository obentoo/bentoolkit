# Pre-commit hooks. Install with:  pip install pre-commit && pre-commit install
# Update hook revs with:           pre-commit autoupdate
repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.30.1
    hooks:
      - id: gitleaks
